# Keycloak OAuth 2.1 Configuration

# Keycloak realm URL (e.g., http://localhost:8080/realms/myrealm)
VITE_AUTH_URL=http://localhost:8080/realms/myrealm

# Public client ID registered in Keycloak
VITE_CLIENT_ID=my-public-client

# Application URL (optional, defaults to window.location.origin)
VITE_APP_URL=http://localhost:5173

# Base path for GitHub Pages deployment (should match vite.config.ts base)
VITE_BASE_PATH=/simple-app
